# 片段结果校验调整

- 建议区间允许由多个连续观察事件覆盖；存在空档则过滤该建议。
- 建议字段错误只过滤对应建议，在 uncertainties 中写入位置及原因。
- 摘要、观察事件等核心结构错误仍使片段失败，数据库保存具体校验位置。
- 对时长至少10秒且所有事件结束时间数值均不超过片段秒数的响应，保守标记时间单位待核对；保留摘要，隐藏事件和建议，不自动乘1000。此启发式可能误判开头极短事件，也不能识别全部单位错误，后续仍需人工或更强校验。
- 提示词明确毫秒示例、issue 字段名和连续证据规则；提示词属于执行配置快照，新旧快照不同，不能假设重试复用旧产物。
- 18项针对性测试通过，包含已有3份云端响应的离线重放，未新增模型请求。
- 原失败任务未被改写；离线解析通过不代表已核实画面内容或整条链路完成。
